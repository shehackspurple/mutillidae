# Starter pipeline

# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- task: Bash@3
  inputs:
    targetType: 'inline'
    script: |
      # Write your commands here
- task: Bash@3
  inputs:
    targetType: 'inline'
    script: |
      # Write your commands here
      
      echo 'Hello SemGrep!'
      
      python3 -m pip install semgrep

      semgrep --config=p/r2c-ci $(Build.SourcesDirectory))
      echo '**************Attempted to scan Build.SourcesDirectory ***************'

      git clone https://dev.azure.com/hstm-corp/Checkmarx-TFVC/_versionControl?path=%24/Checkmarx-TFVC/NARP
      unzip *.zip
      semgrep --config=p/r2c-ci Checkmarx-TFVC/NARP/Main/Code/

      echo 'Bye bye SemGrep!'

